import os

# =========================================================
# 기본 경로 설정
# =========================================================
# 프로젝트 루트 디렉토리 (현재 파일 기준 상위 디렉토리)
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
# 프로젝트 최상위 루트 (Trademark 폴더)
PROJECT_ROOT = os.path.dirname(BASE_DIR)

# =========================================================
# 모델 관련 설정
# =========================================================
# YOLO v11 모델 경로 (상대 경로 설정)
# 현재 폴더(model1_full) 내의 yolo 폴더 안의 best.pt를 참조합니다.
# 팀원들에게 공유 시 model1_full 폴더만 전달하면 바로 동작합니다.
YOLO_MODEL_PATH = os.path.join(BASE_DIR, "yolo", "best.pt")

# SAM 2 모델 설정
# 모델 이름 (sam2.1_b.pt: Base 모델 추천)
# 처음 실행 시 ultralytics가 자동으로 다운로드합니다.
SAM_MODEL_NAME = "sam2.1_b.pt"

# =========================================================
# 감지(Detection) 설정
# =========================================================
# Confidence Threshold (확신 점수 컷)
# 0.0 ~ 1.0 사이 값. 이 점수 이상인 박스만 상표로 인정
CONF_THRESHOLD = 0.5

# IOU Threshold (겹침 허용치)
# 중복된 박스를 제거할 때 사용 (보통 0.4~0.5 사용)
IOU_THRESHOLD = 0.45

# 바운딩 박스 확장 비율 (Padding)
# SAM에게 전달하기 전, 박스를 상하좌우로 얼마나 넓힐지 설정 (0.1 = 10% 확장)
# 너무 딱 맞는 박스보다 약간 여유 있는 박스가 SAM 인식률을 높일 수 있음
BBOX_EXPANSION_RATIO = 0.1

# =========================================================
# 결과 저장 경로 설정
# =========================================================
# 결과 이미지가 저장될 폴더 이름
OUTPUT_DIR_NAME = "output_results"
OUTPUT_DIR = os.path.join(BASE_DIR, OUTPUT_DIR_NAME)

# 테스트할 이미지가 있는 폴더 (상대 경로 설정)
# 현재 폴더(model1_full) 내의 "상표이미지 테스트용" 폴더를 참조합니다.
TEST_IMAGE_DIR = os.path.join(BASE_DIR, "상표이미지 테스트용")
